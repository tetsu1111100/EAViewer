<UserControl x:Class="EAViewer.Views.Controls.TableGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Width="400" MinWidth="260">

    <Border CornerRadius="8" 
            BorderThickness="1" 
            BorderBrush="#D0D0D0"
            Background="White"
            Effect="{StaticResource GridShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar (Draggable) -->
            <Border Grid.Row="0" 
                    x:Name="TitleBar"
                    CornerRadius="8,8,0,0" 
                    Padding="12,8"
                    Cursor="SizeAll"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                    MouseMove="TitleBar_MouseMove"
                    MouseLeftButtonUp="TitleBar_MouseLeftButtonUp">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#0078D4" Offset="0"/>
                        <GradientStop Color="#106EBE" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               Text="{Binding DisplayTitle}" 
                               Foreground="White" 
                               FontWeight="SemiBold" 
                               FontSize="13"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding DisplayTitle}"
                               VerticalAlignment="Center"/>
                               
                    <ui:Button Grid.Column="1"
                               Command="{Binding CloseCommand}"
                               Appearance="Transparent"
                               Background="Transparent"
                               Foreground="White"
                               Icon="{ui:SymbolIcon Dismiss20}"
                               Padding="2"
                               Margin="4,-4,-8,-4"
                               VerticalAlignment="Center"
                               Cursor="Hand"
                               ToolTip="關閉表格"/>
                </Grid>
            </Border>

            <!-- Data Grid -->
            <DataGrid Grid.Row="1"
                      x:Name="DetailGrid"
                      ItemsSource="{Binding Rows}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeColumns="True"
                      CanUserResizeRows="False"
                      CanUserSortColumns="False"
                      CanUserReorderColumns="False"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      HorizontalGridLinesBrush="#E8E8E8"
                      VerticalGridLinesBrush="#E8E8E8"
                      RowHeight="26"
                      BorderThickness="0"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFAFA"
                      FontSize="12"
                      IsReadOnly="True">

                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#1E90FF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#1E90FF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                </DataGrid.Resources>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F0F0F0"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="BorderBrush" Value="#D0D0D0"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <!-- ColName -->
                    <DataGridTemplateColumn Header="欄位" Width="120" MinWidth="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding ColNameBackground}" Padding="4,2" Margin="-1"
                                        PreviewMouseLeftButtonDown="Cell_PreviewMouseLeftButtonDown">
                                    <TextBlock Text="{Binding DisplayColName}" 
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding ColNameTooltip}"
                                               ToolTipService.ShowDuration="15000"
                                               Tag="ColName"
                                               MouseRightButtonDown="Cell_MouseRightButtonDown">
                                        <TextBlock.ContextMenu>
                                            <ContextMenu Tag="ColName" Opened="ContextMenu_Opened"/>
                                        </TextBlock.ContextMenu>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- ColDesc -->
                    <DataGridTemplateColumn Header="說明" Width="140" MinWidth="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding ColDescBackground}" Padding="4,2" Margin="-1"
                                        PreviewMouseLeftButtonDown="Cell_PreviewMouseLeftButtonDown">
                                    <TextBlock Text="{Binding ColDesc}" 
                                               TextTrimming="CharacterEllipsis"
                                               Tag="ColDesc"
                                               MouseRightButtonDown="Cell_MouseRightButtonDown">
                                        <TextBlock.ToolTip>
                                            <StackPanel MaxWidth="300">
                                                <TextBlock Text="{Binding ColName}" FontWeight="Bold" Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding ColDesc}" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </TextBlock.ToolTip>
                                        <TextBlock.ContextMenu>
                                            <ContextMenu Tag="ColDesc" Opened="ContextMenu_Opened"/>
                                        </TextBlock.ContextMenu>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Remark -->
                    <DataGridTemplateColumn Header="備註" Width="120" MinWidth="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding RemarkBackground}" Padding="4,2" Margin="-1"
                                        PreviewMouseLeftButtonDown="Cell_PreviewMouseLeftButtonDown">
                                    <TextBlock Text="{Binding Remark}" 
                                               TextTrimming="CharacterEllipsis"
                                               Tag="Remark"
                                               MouseRightButtonDown="Cell_MouseRightButtonDown">
                                        <TextBlock.ToolTip>
                                            <StackPanel MaxWidth="300">
                                                <TextBlock Text="{Binding ColName}" FontWeight="Bold" Margin="0,0,0,4"/>
                                                <TextBlock Text="{Binding Remark}" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </TextBlock.ToolTip>
                                        <TextBlock.ContextMenu>
                                            <ContextMenu Tag="Remark" Opened="ContextMenu_Opened"/>
                                        </TextBlock.ContextMenu>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Footer -->
            <Border Grid.Row="2" 
                    CornerRadius="0,0,8,8" 
                    Padding="8,6"
                    Background="#F8F8F8"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="Copy Table Name"
                               Icon="{ui:SymbolIcon Copy24}"
                               Appearance="Secondary"
                               Command="{Binding CopyTableNameCommand}"
                               FontSize="11"
                               Padding="8,4"
                               Margin="0,0,8,0"/>
                    <ui:Button Content="Save Background Color"
                               Icon="{ui:SymbolIcon Save24}"
                               Appearance="Primary"
                               Command="{Binding SaveBackgroundColorCommand}"
                               FontSize="11"
                               Padding="8,4"/>
                </StackPanel>
            </Border>

            <!-- Resize Thumb -->
            <Thumb Grid.Row="0" Grid.RowSpan="3"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Width="16" Height="16"
                   Cursor="SizeNWSE"
                   Background="Transparent"
                   DragDelta="ResizeThumb_DragDelta">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Grid Background="Transparent">
                            <Path Data="M 12,16 L 16,16 L 16,12 Z  M 8,16 L 16,16 L 16,8 Z"
                                  Fill="#A0A0A0"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Bottom"/>
                        </Grid>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </Grid>
    </Border>
</UserControl>
